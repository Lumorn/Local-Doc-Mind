system:
  cpu_offload: true # Erlaubt Auslagerung in RAM wenn VRAM voll
  use_flash_attn: false # Standardmaessig aus auf Windows
paths:
  input: "./input"
  output: "./output"
  backup: "./backup"
  models: "./models"
models:
  ocr:
    repo: "deepseek-ai/DeepSeek-OCR-2"
    quantization: "4bit" # Zwingend fuer Consumer GPUs
  llm:
    repo: "Qwen/Qwen2.5-7B-Instruct"
